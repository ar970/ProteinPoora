{
  "name": "proteinpoora",
  "version": "1.0.0",
  "private": true,
  "description": "Protein पूरा — storefront and pre-order API.",
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "pg": "^8.13.1",
    "razorpay": "^2.9.8"
  },
  "scripts": {
    "check:prices": "node scripts/check-prices.js",
    "dev": "node scripts/dev-server.js",
    "test:payments": "bash scripts/test-payments.sh",
    "check:assets": "node scripts/check-assets.js",
    "check:supabase": "node scripts/check-supabase.mjs",
    "check": "npm run check:prices && npm run check:assets"
  }
}
